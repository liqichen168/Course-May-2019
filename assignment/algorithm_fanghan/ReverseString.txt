public void reverseString(char[] s) {
        Stack<Character> result=new Stack<Character>();
        for(char c:s){
            result.push(c);
        }
        for(int i=0;i<s.length;i++){
            s[i]=result.pop();
        }
}