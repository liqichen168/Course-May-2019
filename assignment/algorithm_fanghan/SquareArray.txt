public int[] sortedSquares(int[] A) {
       for(int i=0;i<A.length;i++){
           if(A[i]<0){
               A[i]=(0-A[i])*(0-A[i]);
           }else{
               A[i]=A[i]*A[i];
           }
       }
       Arrays.sort(A);
       return A;
}